{
  "name": "@olad/lint-monorepo",
  "private": true,
  "author": "zhaoxm",
  "license": "ISC",
  "scripts": {
    "release": "bumpp package.json packages/*/package.json --commit --push --tag && pnpm -r publish --access public",
    "publish": "bumpp package.json packages/*/package.json --commit --push && pnpm -r publish --access public"
  },
  "devDependencies": {
    "@olad/eslint-config-typescript": "*",
    "bumpp": "^7.1.1",
    "eslint": "^7.32.0"
  },
  "version": "0.0.2"
}
